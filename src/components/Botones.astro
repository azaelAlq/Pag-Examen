---
interface Props {
  botones: string[];
}

const { botones } = Astro.props;
---

<li>
  {
    botones.map((boton) => (
      <button class="p-2 bg-blue-500 text-white rounded-md m-2">{boton}</button>
    ))
  }
</li>
